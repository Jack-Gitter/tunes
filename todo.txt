if the refresh token is expired, then we just need to send 401 uauthorized request or something. Otherwise can we just send a redirect from the server back to the client for the same webpage with a set-cookie? not sure
